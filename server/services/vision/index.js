const {
  calculateHistogramDiff,
  calculateSSIMDiff,
  calculatePerceptualHashDiff
} = require('./frameDiff');

module.exports = {
  calculateHistogramDiff,
  calculateSSIMDiff,
  calculatePerceptualHashDiff
};
