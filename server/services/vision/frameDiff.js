async function calculateHistogramDiff(frameAPath, frameBPath) {
  void frameAPath;
  void frameBPath;

  // TODO: histogram diff, scene change.
  return 0;
}

async function calculateSSIMDiff(frameAPath, frameBPath) {
  void frameAPath;
  void frameBPath;

  // TODO: SSIM, scene change.
  return 0;
}

async function calculatePerceptualHashDiff(frameAPath, frameBPath) {
  void frameAPath;
  void frameBPath;

  // TODO: perceptual hash, scene change.
  return 0;
}

module.exports = {
  calculateHistogramDiff,
  calculateSSIMDiff,
  calculatePerceptualHashDiff
};
